'use strict'


/* eslint-env mocha */
const jsonServerReset = require('.')

describe('json-server-reset', () => {
  it('write this test', () => {
    console.assert(jsonServerReset, 'should export something')
  })
})
